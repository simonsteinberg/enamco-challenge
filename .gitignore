.venv/
__pycache__/
*.pyc
*.pyo
.env
.DS_Store
.ipynb_checkpoints/

# Outputs that the candidate (re)generates locally
artifacts/*
!artifacts/.gitkeep
predictions.csv
score.json

# The candidate package zips we build on our end. (This entry is harmless
# in the candidate's hands -- they just see a stray pattern.)
best-time-to-call-challenge*.zip
mlchal-test*.zip
